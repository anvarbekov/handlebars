{{!-- {{#if myProducts.length}} 
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        {{#each myProducts}}
            <div class="col">
            <div class="card shadow-sm">
                <img src={{image}} alt="">
                <div class="card-body">
                    <p class="card-text fw-bold">{{title}}</p>
                    <p class="card-text">{{description}}</p>
                   <div class="d-flex justify-content-between align-items-center">
                     <div class="d-flex align-items-center gap-2">
                        <div class="avatar">
                            <p style="margin: 0; padding: 0;">{{getFullNameFirstCharacter user.firstname user.lastname}}</p>
                        </div>
                        <p class="p-0 m-0">{{user.firstname}}</p>
                     </div>
                        <small class="text-muted text-end">{{price}} ming</small>
                   </div>
                </div>
            </div>
            </div>
        {{/each}}
    </div>
{{else}}
    <p>No products</p>
{{/if}} --}}



{{#if myProducts.length}} 
<div class="row row-cols-1 justify-content-center row-cols-sm-2 row-cols-md-3 gap-3">
    {{#each myProducts}}
    <div class="card__content ">
        <div class="card__img-box">
            <a href="cource.html"><img class="card__img" src={{image}}></a>
            <div class="new">New</div>
        </div>
        <h3 class="card__title">{{title}}</h3>
        <div class="card-footer">
            <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
                <a href="/product/{{_id}}" type="button" class="button">Detail</a>
                {{#if @root.token}}
                    {{#ifequal user @root.userId}}
                    <a href="/edit-product/{{_id}}" type="button" class="btn btn-outline-secondary">Edit</a>
                    <form action="/delete-product/{{_id}}" method="post">
                        <button type="submit" class="btn btn-outline-danger">Delete</button>
                    </form>
                    {{/ifequal}}
                {{/if}}
                
            </div>
        </div>
    </div>
 {{/each}}
</div>
{{else}}
<div class="empty">
    <p>No Products 😫</p>
</div>
{{/if}}